{{/*
Namespace-local auth middlewares are rendered centrally from
k8s/charts/haac-stack/templates/auth-middlewares.yaml so Gateway API
HTTPRoute ExtensionRef filters do not depend on cross-namespace
Traefik CRD name conventions.
*/}}
