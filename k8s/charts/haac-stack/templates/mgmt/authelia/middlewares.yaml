{{- range $ns := (list "mgmt" "monitoring" "media" "security" "chaos") }}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: authelia
  namespace: {{ $ns }}
spec:
  forwardAuth:
    address: http://authelia.mgmt.svc.cluster.local:9091/api/authz/forward-auth?strategy=header
    trustForwardHeader: true
    authResponseHeaders:
      - Remote-User
      - Remote-Groups
      - Remote-Name
      - Remote-Email
{{- end }}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: force-https
  namespace: mgmt
spec:
  headers:
    customRequestHeaders:
      X-Forwarded-Proto: "https"
