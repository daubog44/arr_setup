{{- if .Values.global.scheduling.gpu.nvidiaDevicePluginEnabled }}
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
  annotations:
    argocd.argoproj.io/sync-wave: "1"
handler: nvidia
{{- end }}
