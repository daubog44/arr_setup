## 1. Stable Spec Sync

- [x] 1.1 Sync the accepted requirements from the completed change delta specs into stable specs under `openspec/specs/`
- [x] 1.2 Resolve any overlap between the completed loop self-improvement deltas and record the chosen merged baseline

## 2. Archive And Reference Cleanup

- [x] 2.1 Archive the completed changes under `openspec/changes/archive/` after stable spec sync is complete
- [x] 2.2 Update repo docs and loop prompts so completed changes are no longer described as active work

## 3. Validation And Closeout

- [x] 3.1 Validate the new change with `openspec validate archive-complete-openspec-capabilities`
- [x] 3.2 Record the evidence, resulting archive paths, and the new steady-state reference points in the worklog
