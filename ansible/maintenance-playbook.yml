---
# Compatibility aggregate playbook for manual maintenance runs.
# Semaphore uses the split playbooks below for strict host-class isolation:
#   - ansible/maintenance-k3s-playbook.yml
#   - ansible/maintenance-proxmox-playbook.yml
- import_playbook: maintenance-k3s-playbook.yml
- import_playbook: maintenance-proxmox-playbook.yml
